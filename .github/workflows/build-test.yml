name: build-test
on:
  workflow_call:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            profile: linux
          - os: macos-latest
            profile: macos
          - os: windows-latest
            profile: windows
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: conan-io/setup-conan@v1
        with:
          version: 2.9.0

      - name: Install dependencies (clang-format, cmake)
        run: |
          if [ "${{ runner.os }}" == "Linux" ]; then
            sudo apt-get update && sudo apt-get install -y clang-format cmake ninja-build
          elif [ "${{ runner.os }}" == "macOS" ]; then
            brew install clang-format cmake ninja
          fi
        shell: bash

      - name: Format check (clang-format)
        run: |
          find src include tests -name "*.cpp" -o -name "*.h" | xargs clang-format -i
          git diff --exit-code || (echo "Format check failed. Please run clang-format."; exit 1)
        shell: bash

      - name: Conan install & generate toolchain
        run: |
          conan install . --profile=profiles/${{ matrix.profile }} --build=missing -s compiler.version=16
        if: runner.os == 'macOS'

      - name: Conan install & generate toolchain (Linux)
        run: |
          conan install . --profile=profiles/${{ matrix.profile }} --build=missing
        if: runner.os == 'Linux'

      - name: Conan install & generate toolchain (Windows)
        run: |
          conan install . --profile=profiles/${{ matrix.profile }} --build=missing
        if: runner.os == 'Windows'

      - name: Configure CMake
        run: |
          cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE=build/Release/generators/conan_toolchain.cmake

      - name: Build
        run: |
          cmake --build build --config Release

      - name: Run unit tests
        run: |
          ctest --test-dir build --output-on-failure
